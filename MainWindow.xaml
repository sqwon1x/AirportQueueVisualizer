<Window x:Class="AirportQueueVisualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AirportQueueVisualizer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Візуалізатор черги в аеропорту"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="NameBox" Width="150" Margin="5" />
            <TextBox x:Name="FlightBox" Width="150" Margin="5" />
            <Button Content="Додати" Width="100" Margin="5" Click="Add_Click"/>
        </StackPanel>

        <ListBox x:Name="PassengerList" Grid.Row="2" Margin="5"/>

        <Button Content="Видалити" Grid.Row="3" Width="100" Margin="5" Click="Delete_Click"/>
    </Grid>
</Window>
