<Page x:Class="AirportQueueVisualizer.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AirportQueueVisualizer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0.0" To="1.0" Duration="0:0:0.5"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Grid Margin="30">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="600">
            <TextBlock Text="{DynamicResource HomeWelcome}"
                       Style="{DynamicResource HeaderStyle}"
                       FontSize="32"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="{DynamicResource HomeDesc}"
                       FontSize="16"
                       Foreground="{DynamicResource TextColor}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       LineHeight="24"
                       Margin="0,0,0,30"/>

            <Border Background="{DynamicResource PanelColor}" BorderBrush="{DynamicResource AccentColor}" BorderThickness="2" CornerRadius="8" Padding="20" Margin="0,0,0,30">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{DynamicResource HomeStats}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextColor}"/>

                    <TextBlock x:Name="TotalPassengerLabel" Text="0" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentColor}"/>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource BgColor}" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="{DynamicResource HomeQuickStart}" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource TextColor}" Margin="0,0,0,10"/>
                    <TextBlock Text="{DynamicResource HomeStartDesc}" FontSize="14" Foreground="{DynamicResource TextColor}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
